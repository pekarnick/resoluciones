<?php

class ResolucionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('resolucions')->truncate();

		$resolucions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('resolucions')->insert($resolucions);
	}

}

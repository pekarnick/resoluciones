<?php

class NombresTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('nombres')->truncate();

		$nombres = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('nombres')->insert($nombres);
	}

}
